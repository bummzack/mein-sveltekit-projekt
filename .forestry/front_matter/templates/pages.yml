---
label: Pages
hide_body: false
fields:
- name: title
  type: text
  config:
    required: false
  label: Title
- name: image
  type: file
  config:
    maxSize: 64
  label: Image
- name: imageClass
  type: select
  default: []
  config:
    required: false
    options:
    - wide
    - narrow
    - blurred
    source:
      type: simple
      section: 
      file: 
      path: 
  label: Image Class
- name: blocks
  type: blocks
  label: blocks
  template_types:
  - teaser
  config:
    min: 
    max: 
